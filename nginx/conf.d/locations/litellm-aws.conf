# Legacy LiteLLM AWS entry — redirect to unified LiteLLM documentation center
location = /litellm-aws {
    absolute_redirect off;
    return 301 /litellm/;
}

location = /litellm-aws/ {
    absolute_redirect off;
    return 302 /litellm/reports/aws-deployment-architecture-phase2.html;
}

location /litellm-aws/ {
    absolute_redirect off;
    return 302 /litellm/reports/aws-deployment-architecture-phase2.html;
}
