# code-reviewer public docs
# absolute_redirect off 確保透過 ngrok 時 redirect 不走絕對路徑（避免 port 錯）
location = /code-review {
    absolute_redirect off;
    return 301 /code-review/;
}

location /code-review/ {
    alias /doc-sites/code-reviewer/;
    index index.html;
    autoindex off;
}
