# absolute_redirect off 確保透過 ngrok 時 redirect 不走絕對路徑（避免 port 錯）
location = /company-profile {
    absolute_redirect off;
    return 301 /company-profile/;
}

location /company-profile/ {
    alias /doc-sites/company-profile/;
    index index.html;
}
